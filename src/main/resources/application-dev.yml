spring:
  datasource:
    url: jdbc:mysql://db-35uo47-kr.vpc-pub-cdb.ntruss.com:3306/pinggyewang_dev?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: swyp
    password: tmdnlvm7!

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create-drop  # 개발환경에서는 매번 테이블 재생성
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

clova:
  api:
    url: https://clovastudio.stream.ntruss.com/testapp/v3/chat-completions/HCX-005
    key: nv-d6eadc1a642f46d2834ffdb0c1e11e07flHE

server:
  port: 8080