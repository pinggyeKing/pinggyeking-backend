spring:
  application:
    name: pinggye-wang
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.swyp10.pinggyewang: DEBUG
    org.springframework.security: DEBUG